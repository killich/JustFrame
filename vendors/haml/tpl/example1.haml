!!! 1.1
%html
  %head
    %title Example #1
    %meta{ :http-equiv => 'Content-Type', :content => 'application/xhtml+xml; charset=utf-8' }
    %link{ :rel => 'stylesheet', :type => 'text/css', :href => 'tpl/main.css' }
  %body
    %table
      /[if IE]
        %p Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. Foo bar. 
      / Lorem ipsum
      / Bez nagłówka, ciekawe jak to wygląda? W rzeczywistości to tylko test komentarza
        %tr
          %th ID
          %th Name
          %th Value
      - foreach ($models as $model)
        %tr[$model]
          %td= $model->ID
          %td= $model->name
          %td= $model->value